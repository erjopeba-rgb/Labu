# ============================================================
# Labu — Variables de entorno para PRODUCCIÓN
# Copiar como .env en el servidor y rellenar con valores reales.
# NUNCA commitear el .env real al repositorio.
# ============================================================

# ── Servidor ─────────────────────────────────────────────────
NODE_ENV=production
PORT=3000

# Dominio base de la app (sin trailing slash)
APP_URL=https://tudominio.com

# CORS: dominios permitidos separados por coma
CORS_ORIGIN=https://tudominio.com,https://www.tudominio.com

# ── Base de datos ─────────────────────────────────────────────
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=rtype1
DB_USER=rtype1_user
DB_PASSWORD=contraseña_segura_aqui
DB_SSL=false

# Pool de conexiones
DB_POOL_MIN=2
DB_POOL_MAX=10

# ── Redis ─────────────────────────────────────────────────────
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
# REDIS_TLS=true  # descomentar si Redis usa TLS

# ── Auth / JWT ────────────────────────────────────────────────
JWT_SECRET=cadena_aleatoria_larga_minimo_64_caracteres_aqui
JWT_EXPIRES_IN=7d

# ── Email (SMTP) ──────────────────────────────────────────────
SMTP_HOST=smtp.tuproveedor.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@tudominio.com
SMTP_PASS=contraseña_smtp_aqui
SMTP_FROM="Labu <noreply@tudominio.com>"

# ── MercadoPago ───────────────────────────────────────────────
MP_ACCESS_TOKEN=APP_USR-0000000000000000-000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-000000000
MP_PUBLIC_KEY=APP_USR-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
MP_WEBHOOK_SECRET=tu_clave_secreta_webhook_mercadopago

# ── AWS S3 (uploads) ──────────────────────────────────────────
AWS_REGION=sa-east-1
AWS_S3_BUCKET=rtype1-uploads-prod
AWS_ACCESS_KEY_ID=tu_access_key_id_aqui
AWS_SECRET_ACCESS_KEY=tu_secret_access_key_aqui

# ── Backups ───────────────────────────────────────────────────
# Bucket separado para backups (puede ser el mismo que uploads)
BACKUP_S3_BUCKET=rtype1-backups-prod
BACKUP_EMAIL_ALERT=admin@tudominio.com

# ── Logging ───────────────────────────────────────────────────
LOG_LEVEL=info
# Opciones: trace | debug | info | warn | error | fatal

# ── OSRM (routing para agendamiento) ─────────────────────────
# URL de la instancia OSRM. Usar la pública solo para MVP.
# En producción con volumen alto: levantar instancia propia.
OSRM_URL=http://router.project-osrm.org

# ── Seguridad adicional ────────────────────────────────────────
# Clave para endpoints de admin (header X-Admin-Key)
ADMIN_SECRET_KEY=clave_admin_secreta_aqui
